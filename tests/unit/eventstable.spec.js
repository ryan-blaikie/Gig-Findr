import { shallowMount } from '@vue/test-utils'
import EventTable from '../../Client/src/components/EventTable.vue'

describe('EventTable', () => {
    // Inspect the raw component options
    it('has data', () => {
      expect(typeof App.data).toBe('function')
    })
  })